[![Build Status](https://travis-ci.org/z0mbix/zup.svg?branch=master)](https://travis-ci.org/z0mbix/zup)

zup is a wrapper for unison to easily sync a directory recursively with a remote host usually over ssh.

Requirements:
	unison
	rsync
	ruby
	ssh
